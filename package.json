{
  "name": "zemkit",
  "private": true,
  "scripts": {
    "clean": "turbo clean && rm -rf .turbo node_modules pnpm-lock.yaml",
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@zemkit/eslint-config": "workspace:*",
    "@zemkit/typescript-config": "workspace:*",
    "prettier": "^3.2.5",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}
